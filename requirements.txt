# Deployment Requirements
# These are the minimal packages needed to run the Streamlit dashboard on Streamlit Community Cloud.
# Local development and data modeling (dbt) dependencies are in requirements.dev.txt.

pandas>=2.0
numpy>=1.24
scipy>=1.11
statsmodels>=0.14
plotly>=5.18
streamlit>=1.28
pyarrow
